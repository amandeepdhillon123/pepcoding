//  sequence of character 

let str ="amandeep dhillon";
// console.log(str);
// console.log(str.length);

// 1. slice(start, end+1)

let slice= str.slice(0, 5)
console.log(slice);

// substrirng
let sub = str.substr(1,4);
// console.log(sub)

// replacing value 

let replece = str.replace("amandeep","ran");

console.log(replece);

// to uppecase and lowercase 

let text="hello";

let txt=text.toUpperCase();
console.log(txt);

// concatenation 
let name="amam";
let sur="dhillon";

let con= name.concat(" ",sur);
console.log(con)

